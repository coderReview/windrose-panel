services:
  grafana:
    user: root
    container_name: 'coder-windrose-panel'

    build:
      context: .
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana-enterprise}
        grafana_version: ${GRAFANA_VERSION:-12.3.2}
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
    volumes:
      - ../dist:/var/lib/grafana/plugins/coder-windrose-panel
      - ../provisioning:/etc/grafana/provisioning
      - ..:/root/coder-windrose-panel

    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.coder-windrose-panel:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: coder-windrose-panel
